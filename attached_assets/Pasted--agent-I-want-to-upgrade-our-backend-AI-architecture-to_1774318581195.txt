"@agent I want to upgrade our backend AI architecture to a 'Multi-Model Ensemble' utilizing both OpenAI (GPT-4o) and Anthropic (Claude). Please implement the following architecture changes:

Task 1: Add Anthropic Dependencies & Env Vars
Install the official Anthropic SDK: npm install @anthropic-ai/sdk.

Ensure my backend environment variables now expect both OPENAI_API_KEY and ANTHROPIC_API_KEY.

Task 2: Create a Unified AI Service Router
Create or update server/services/ai.ts (or similar AI utility file) to export a centralized AI routing function. It should look something like this:

TypeScript
import OpenAI from 'openai';
import Anthropic from '@anthropic-ai/sdk';

const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });
const anthropic = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });

export async function generateAIResponse(prompt: string, taskType: 'data_analysis' | 'customer_chat' | 'legal_review') {
  try {
    // Route to Claude for human interaction and legal text
    if (taskType === 'customer_chat' || taskType === 'legal_review') {
      const response = await anthropic.messages.create({
        model: 'claude-3-5-sonnet-20241022',
        max_tokens: 1024,
        messages: [{ role: 'user', content: prompt }]
      });
      return response.content[0].text;
    } 
    
    // Route to GPT-4o for heavy data and structured JSON
    if (taskType === 'data_analysis') {
      const response = await openai.chat.completions.create({
        model: 'gpt-4o',
        messages: [{ role: 'user', content: prompt }]
      });
      return response.choices[0].message.content;
    }
  } catch (error) {
    // Implement Fallback logic here if the primary model fails!
    console.error(`Primary model failed, attempting fallback...`, error);
    // Add logic to swap from OpenAI to Anthropic or vice versa
  }
}
Task 3: Refactor Existing AI Routes
Update the endpoints in server/routes.ts (specifically /api/ai/portfolio-intelligence and /api/public/chat) to use this new generateAIResponse router function.

The Portfolio Intelligence should pass 'data_analysis' as the task type so it goes to OpenAI.

The Chatbots should pass 'customer_chat' so they are handled by Claude.

Please implement this unified AI routing logic and confirm when the backend is successfully utilizing both models!"