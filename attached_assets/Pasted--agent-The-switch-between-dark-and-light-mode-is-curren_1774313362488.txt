@agent The switch between dark and light mode is currently too abrupt. I want to add a premium, smooth transition effect using the View Transitions API and CSS fallbacks. Please apply the following two fixes:

Upgrade 1: Add the View Transitions Logic
File: client/src/components/theme-provider.tsx
Action: We need to wrap the theme state update in document.startViewTransition so the browser can smoothly animate the DOM change.

Update the imports at the top to include flushSync:

TypeScript
import { useState, useEffect, createContext, useContext } from "react";
import { flushSync } from "react-dom";
Replace the existing toggleTheme function with this updated version:

TypeScript
  const toggleTheme = () => {
    const newTheme = theme === "light" ? "dark" : "light";
    
    // Fallback for browsers that don't support View Transitions
    if (!document.startViewTransition) {
      setTheme(newTheme);
      return;
    }

    // Premium circular wipe animation for modern browsers
    document.startViewTransition(() => {
      flushSync(() => {
        setTheme(newTheme);
      });
    });
  };
Upgrade 2: Add the CSS Animation Keyframes & Fallback Transitions
File: client/src/index.css
Action: We need to define the animation behavior for the View Transition, and add a fallback transition-colors for standard elements.

Find the @layer base section and update the body styling to include a smooth color transition fallback:

CSS
  body {
    @apply font-sans antialiased bg-background text-foreground transition-colors duration-500 ease-in-out;
    /* ... keep the existing background-image properties here ... */
Scroll to the very bottom of index.css and append these specific View Transition keyframes:

CSS
/* ========================================================
   PREMIUM THEME TOGGLE ANIMATION (VIEW TRANSITIONS API)
   ======================================================== */
::view-transition-old(root),
::view-transition-new(root) {
  animation: none;
  mix-blend-mode: normal;
}

::view-transition-old(root) {
  z-index: 1;
}

::view-transition-new(root) {
  z-index: 2;
  animation: theme-wipe 0.6s cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

.dark::view-transition-old(root) {
  z-index: 2;
  animation: theme-wipe-reverse 0.6s cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

.dark::view-transition-new(root) {
  z-index: 1;
  animation: none;
}

@keyframes theme-wipe {
  from {
    clip-path: circle(0% at top right);
  }
  to {
    clip-path: circle(150% at top right);
  }
}

@keyframes theme-wipe-reverse {
  from {
    clip-path: circle(150% at top right);
  }
  to {
    clip-path: circle(0% at top right);
  }
}
Please apply these updates and confirm when done!