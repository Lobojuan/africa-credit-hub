@agent I am preparing this enterprise application for a production deployment. I need you to perform a comprehensive "Pre-Flight Check" and apply the necessary optimizations and security audits. Please execute the following 4 tasks sequentially and report back on your progress.

Task 1: Full TypeScript & Build Check
Run the build process to catch any hidden TypeScript, ESLint, or Vite bundling errors.

Execute npm run build (or the equivalent build command in my package.json).

If the build fails, analyze the errors and automatically apply the fixes for any strict type mismatches, missing imports, or unused variables that are blocking the build.

Do not proceed to Task 2 until the app successfully compiles.

Task 2: Optimize Routing with Lazy Loading (Code Splitting)
This is a massive React application with dozens of heavy pages and charting libraries (recharts). Loading the entire bundle upfront will severely impact performance.

Locate my main routing file (likely client/src/App.tsx or client/src/main.tsx).

Refactor the route imports to use React.lazy() for all page components.

Wrap the main routing block (e.g., <Switch> or <Routes>) in a <React.Suspense fallback={<Loader />}> boundary so the app doesn't freeze while loading chunks.
Example transformation:

TypeScript
// Change this:
import Dashboard from "./pages/dashboard";

// To this:
import { lazy, Suspense } from "react";
const Dashboard = lazy(() => import("./pages/dashboard"));
Task 3: Frontend Secrets Audit
Security is paramount for this credit registry platform.

Scan the entire client/src/ directory.

Ensure there are absolutely no hardcoded secret keys (e.g., sk_live_..., sk_test_..., sk-proj-..., database URIs, or JWT secrets) inside the frontend code.

Verify that all frontend environment variables correctly use the VITE_ prefix (e.g., import.meta.env.VITE_COUNTRY_MODE).

If you find any exposed secrets in the frontend, remove them immediately and replace them with API calls to the backend.

Task 4: AI & API Rate Limiting Verification (Backend)
We are using expensive GPT-4o models for credit narratives, dispute chatbots, and portfolio intelligence. We cannot allow malicious users to spam these endpoints.

Scan my backend routes (likely in server/routes.ts or a similar backend directory).

Look for the endpoints starting with /api/ai/ and /api/credit-reports/.

Verify that there is a strict rate-limiting middleware applied to these specific routes (e.g., using express-rate-limit).

If rate limiting is missing, please implement a standard rate limiter for the AI routes (e.g., max 10 requests per 15 minutes per IP/User) to protect against billing abuse.

Please execute these 4 tasks, make the necessary code changes, and give me a final status report when the application is officially clean, optimized, and ready to publish!