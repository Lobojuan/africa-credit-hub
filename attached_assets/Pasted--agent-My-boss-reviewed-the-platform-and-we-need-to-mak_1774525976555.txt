@agent My boss reviewed the platform and we need to make a massive UX and Architectural pivot. We need to strictly separate "Borrowers" into two completely distinct areas: Consumers (B2C) and Businesses (B2B). They must not be mixed in the UI, and access must be filtered based on user roles.

Please execute the following 3 tasks to implement this logical separation:

Task 1: Split the Backend APIs & Enforce RBAC
We need dedicated endpoints so the frontend never downloads mixed data.

In server/routes.ts, leave the master /api/borrowers route for Super Admins, but create two new dedicated endpoints:

GET /api/consumers -> Returns only borrowers where type === 'individual'.

GET /api/businesses -> Returns only borrowers where type === 'corporate'.

Update the User schema/types to support new specific roles if necessary, or ensure that the API checks if a user is assigned to a "Retail" vs "Corporate" division (if you need to add a division field to the users table, please do so). If a Retail user hits the Business API, throw a 403 Forbidden.

Task 2: Split the Frontend Navigation & Routing
In client/src/components/app-sidebar.tsx, remove the generic "Borrowers" link.

Add two new links under the Core section:

Consumers (Icon: User, Route: /consumers)

Businesses (Icon: Building2, Route: /businesses)

Update client/src/App.tsx (or main router) to map these new routes.

Task 3: Build the Hyper-Focused, Clean UIs
We need to stop drowning users in data. We need two clean, distinct pages.

Create client/src/pages/consumers.tsx:

This page should only fetch from /api/consumers.

The table columns should be strictly human-focused: Name, National ID, Credit Score, Employer, Total Debt.

Remove any mention of "Sector", "Incorporation Date", or "Company Reg No".

Create client/src/pages/businesses.tsx:

This page should only fetch from /api/businesses.

The table columns should be strictly business-focused: Company Name, TIN / Reg No, Sector, Credit Score, Total Exposure.

Remove any mention of "Gender", "Date of Birth", or "Employer".

Refactor the Detail Views: Create consumer-detail.tsx and business-detail.tsx (or severely refactor the existing borrower-detail.tsx with strict conditional rendering).

The Business view must prominently feature "Directors / Beneficial Owners" and "Commercial Facilities".

The Consumer view must focus on "Personal Loans", "Mortgages", and "Employment History".