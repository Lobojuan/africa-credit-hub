@agent The core application is secure and optimized, but before we deploy this enterprise fintech platform to production, I need you to implement these final 3 operational and performance upgrades.

Task 1: Express Server Production Hardening
Because this handles sensitive financial data, our Node/Express server serving the Vite frontend needs strict security headers and payload compression.

Check server/index.ts (or wherever the main Express app is initialized).

Ensure the helmet package is installed and configured to set secure HTTP headers (e.g., HSTS, X-Content-Type-Options, X-Frame-Options).

Ensure the compression package is installed and used as middleware (app.use(compression())) to gzip our API payloads and static assets, reducing bandwidth for users on slower African networks.

Ensure a global error handler is in place so that unhandled promise rejections or API crashes log the error safely but return a generic 500 Internal Server Error to the client instead of leaking stack traces.

Task 2: PWA (Progressive Web App) Completion
I noticed we have a PWAInstallPrompt component in the frontend, which means users can install this registry to their home screens. For this to actually trigger in Chrome/Safari, we need a valid manifest and service worker.

Check the client/public/ directory. Ensure there is a valid manifest.json file with a name, short_name, theme_color (use #115e59 for the teal theme), background_color, and display: "standalone".

Create a basic empty Service Worker file (e.g., sw.js or service-worker.js) in the public folder if it doesn't exist.

Ensure index.html has the proper <link rel="manifest" href="/manifest.json"> tag and a <script> tag to register the service worker on load.

Task 3: Database Seed Script for "Free Trials"
Our landing page advertises a 14-day free trial loaded with sample data so new clients can test the dashboard, AI features, and reports immediately.

Check if we have a seed.ts or db/seed.ts script.

If we do not have one, please create a basic server/seed.ts script that uses Drizzle ORM to inject a few mock Borrowers, Credit Accounts, and Institutions into the database.

Add a script command to package.json like "seed": "tsx server/seed.ts" so I can easily populate a fresh production database when a new client signs up.

Please implement these final 3 tasks and let me know when they are complete. If you need to install any new npm packages (like helmet or compression), please do so.