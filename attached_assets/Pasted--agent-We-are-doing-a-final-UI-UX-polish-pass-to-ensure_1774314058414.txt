@agent We are doing a final UI/UX polish pass to ensure the application feels incredibly smooth, tactile, and bug-free across all devices. Please apply the following 4 experiential upgrades:

Upgrade 1: Premium Universal Scrollbars
File: client/src/index.css
Issue: Default browser scrollbars break the immersion of the dark/light themes, especially on Windows/Linux, and the current custom scrollbars are a bit rigid.
Action: Replace the existing ::-webkit-scrollbar block at the bottom of the file with this refined, floating scrollbar implementation:

CSS
  /* Premium Scrollbars */
  ::-webkit-scrollbar {
    width: 8px;
    height: 8px;
  }
  ::-webkit-scrollbar-track {
    background: transparent;
  }
  ::-webkit-scrollbar-thumb {
    background: hsl(var(--muted-foreground) / 0.2);
    border-radius: 10px;
    border: 2px solid transparent;
    background-clip: padding-box;
    transition: all 0.2s ease;
  }
  ::-webkit-scrollbar-thumb:hover {
    background: hsl(var(--muted-foreground) / 0.4);
    border: 2px solid transparent;
    background-clip: padding-box;
  }
  ::-webkit-scrollbar-corner {
    background: transparent;
  }
Upgrade 2: Tactile Feedback on Buttons and Cards
File: client/src/components/ui/button.tsx and client/src/index.css
Issue: When users click buttons or cards, there should be a subtle physical "press" effect.
Action: 1. In client/src/components/ui/button.tsx, add active:scale-[0.98] transition-transform duration-200 to the base buttonVariants classes.
2. In client/src/index.css, locate the .hover-elevate and .card-shine definitions, and ensure they include active:scale-[0.98] or similar tactile active states so that when a user taps a borrower card on mobile, it visually depresses slightly.

Upgrade 3: Fix Dashboard Layout Shifts (CLS)
File: client/src/pages/dashboard.tsx
Issue: When the dashboard loads, the Skeleton loaders are h-16, but the actual StatCards render at about h-[120px]. This causes the page to violently jump down when the API finishes loading.
Action: Find the statsLoading render block in dashboard.tsx:

TypeScript
        {statsLoading ? (
          Array.from({ length: 8 }).map((_, i) => (
            <Card key={i} className="border border-border/60"><CardContent className="p-5"><Skeleton className="h-[100px] w-full" /></CardContent></Card>
          ))
        ) : stats ? (
Change the Skeleton height from h-16 to h-[100px] (or matching the actual card height) to prevent layout shifting. Do the same for the skeletons in client/src/pages/platform-metrics.tsx.

Upgrade 4: Buttery Smooth Mobile Tables
File: client/src/components/ui/table.tsx
Issue: On mobile, scrolling horizontally through dense data tables can feel sticky or trigger the browser's back/forward swipe gesture.
Action: Update the wrapper div in the Table component to include -webkit-overflow-scrolling: touch via Tailwind classes, and ensure it snaps smoothly:

TypeScript
const Table = React.forwardRef<
  HTMLTableElement,
  React.HTMLAttributes<HTMLTableElement>
>(({ className, ...props }, ref) => (
  <div className="relative w-full overflow-x-auto overflow-y-hidden rounded-lg border border-border/50 bg-card/50 overscroll-x-contain touch-pan-x">
    <table
      ref={ref}
      className={cn("w-full caption-bottom text-sm whitespace-nowrap md:whitespace-normal", className)}
      {...props}
    />
  </div>
))
Upgrade 5: Prevent Toast & Chatbot Collisions
File: client/src/components/ui/toast.tsx
Issue: On mobile, the Toast notifications pop up at the bottom right, exactly where the floating Public Chatbot button is, making them unreadable.
Action: In ToastViewport, adjust the mobile positioning classes so it floats at the top of the screen on mobile, but stays at the bottom-right on desktop:

TypeScript
const ToastViewport = React.forwardRef<
  React.ElementRef<typeof ToastPrimitives.Viewport>,
  React.ComponentPropsWithoutRef<typeof ToastPrimitives.Viewport>
>(({ className, ...props }, ref) => (
  <ToastPrimitives.Viewport
    ref={ref}
    className={cn(
      "fixed top-0 z-[100] flex max-h-screen w-full flex-col-reverse p-4 sm:bottom-0 sm:right-0 sm:top-auto sm:flex-col md:max-w-[420px] pointer-events-none",
      className
    )}
    {...props}
  />
))
Please apply these 5 UI/UX polish updates to make the application feel perfectly responsive and stable.