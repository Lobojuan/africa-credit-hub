# Total Code Review — July 2026

Scope: scoring core (deep) + platform (medium). Benchmarked for production readiness.
Companion files: `KNOWN-ISSUES.md` (tracked defects), `ROADMAP.md` (missing features).

## Part A — Platform sweep (COMPLETE)

### Headline numbers
- 220 write endpoints in routes.ts; **112 consume req.body with NO schema validation** (53 use zod, rest ad-hoc or nothing)
- 391 uses of safeErrorMessage() vs **67 raw `e.message` responses** leaking internals (50 in routes.ts + 17 in routes/*.ts + index.ts)
- 128 client pages all routed (no orphans) but **10 command-center subroutes unreachable** by any link
- **47/128 pages (37%) hardcoded English** — no useTranslation
- 6 orphan server files; **4 full source-code ZIPs tracked in git**

### P1 — Unvalidated money/score/PII writes (fix first)
| Line | Route | Risk |
|---|---|---|
| 16824 | PATCH /api/loan-repayments/:id/pay | paidAmount completely unvalidated (money) |
| 13172 | POST /api/payments/initiate | payment + phone PII raw |
| 13120 | POST /api/stripe/checkout | tier drives pricing, raw |
| 12592 | PATCH /api/platform/settlement-accounts/:id | mass-assignment over bank fields |
| 12869 | PUT /api/platform/pricing-tiers/:id | unitPriceCents unvalidated |
| 16770 | POST /api/loan-applications/:id/disburse | disbursement, reference only defaulted |
| 2652 | POST /api/consumer/register | PII destructured raw |
| 6250 | POST /api/credit-reports/generate | borrowerId/purpose/consentId raw |
| 18281 | POST /api/borrowers/merge | destructive merge, no UUID/array validation |
| 18965 | POST /api/consumer/simulate-score | action not enum-checked |

### P2 — Error-message leakage
- 5xx leaking internals: routes.ts:5208 (batch upload), 13472 (AI route)
- ~48 `res.status(400).json({ message: e.message })` catch-alls (1505, 4833, 5415, 6046, …)
- Modular routers: users.ts (4), platform-control.ts (4), regulatory-controls-router.ts (4), auth.ts (1), loto-admin.ts (1), index.ts (3)
- Fix: mechanical sweep → safeErrorMessage() everywhere

### P3 — Navigation / duplication
- 10 unreachable command-center subroutes (App.tsx:301-310) — add tabs to command-center-system.tsx or prune
- /presentation, /partner-docs zero inbound links; /platform-control-9x7k unlinked (confirm if intentional)
- DUPLICATE endpoints: PATCH routes.ts:9816 vs PUT routes.ts:9901 both update ghana-playbook content with DIVERGENT implementations (PATCH writes file, PUT versions in DB) — kill one
- 5 near-identical per-country playbook PATCH handlers (9816, 10214, 10335, 10456, 10577) + 5 near-identical client pages → parameterize to :market, saves ~500 lines

### P4 — Repo hygiene
- Delete orphans: server/demo-sandbox.ts, efd-adapter.ts, seed-large-telco.ts, seed-pan-african.ts, seed-sierra-leone.ts, stripe-seed.ts
- Remove from git: africa-credit-hub.zip, uch-source.zip, exports/ach-source-code.zip, exports/codebase.zip, cdh-codebase-review.txt, 3-day-progress-report.md

### P5 — i18n gaps (CORRECTED 2026-07-07)
CORRECTION: the earlier "locale dictionaries ~50% missing" was a miscount of nested keys.
Actual dictionary coverage (leaf strings vs en's ~2904): fr 100%, pt 98%, ar 96%, sw 98%,
es 99%, zh-CN 104%, zh-TW 117%. **The 7-language dictionary is essentially COMPLETE.**
i18n.ts registers en/fr/pt/ar/sw/es/zh-CN/zh-TW; fr is `typeof en` (key-enforced).

REAL gap = **47/128 pages never call useTranslation** — hardcoded English bypasses the
(complete) dictionary, so those pages render English in every locale. This is a per-page
extraction effort, NOT a dictionary-translation effort.
Consumer/public-facing (HIGH priority — non-English users hit these):
  consumer-portal, consent-respond (legal), country-selection, collections,
  loan-origination, collateral-registry, telco-lending, papss-settlements
Admin/internal (English often acceptable): platform-master-control, organizations,
  data-management, webhook-management, decision-engine, security-compliance, registry-authority-portal
Marketing: presentation, press-kit, ai-demo
Top by visible-English density: platform-master-control(124), collateral-registry(102),
  organizations(75), loan-origination(68), telco-lending(66), collections(62), consumer-portal(61)

### Attack order
1. Zod schemas on the 10 P1 money routes (hours, highest risk reduction)
2. safeErrorMessage sweep (mechanical)
3. Command-center nav tabs or prune
4. Delete dead files + untrack zips
5. i18n per-page chipping


## Part B — Scoring core deep audit (COMPLETE)

### CRITICAL
**F1 — Utilization factor (15% weight) is dead code for every real borrower.**
`credit-score.ts:253-286` reads `a.creditLimit`, but `credit_accounts` has NO creditLimit column (only `telco_credit_scores` has one). Every production call passes raw DB rows → utilization penalty is ALWAYS 0. A borrower with maxed-out facilities loses zero points (up to -120 never applied). Unit tests mask it by passing in-memory objects with creditLimit. Fix: add `credit_limit` column (BoG spec has facility limits) or derive for revolving types; test with real row shape.

### HIGH
**I1 — Every inquiry ever recorded lowers the score — no consent, no soft/hard split, no 12-month window.** All 10 call sites pass `inquiries.length` raw; `consentProvided` defaults false and is never required. The factor text "in the last 12 months" is false — penalties are lifetime. Fix: count only consented, hard, ≤12-month inquiries.
**I2 — Soft-pull endpoint permanently 500s.** `routes.ts:18485` calls `(calculateCreditScore as any)(borrower, accounts, ...)` — arguments shifted by one, hidden by the cast. `borrower.filter` throws. Naive fix would return garbage. Correct call: `calculateCreditScore(accounts, hardInquiries.length, judgments, borrower.isPep ?? false, altData)` + exclude isSoftPull everywhere.
**C1 — Same borrower, different scores depending on the door.** Internal/consumer/PDF paths include altData; external bank API and dashboard omit it (up to ~90 pts lower via API); decision-rules engine omits altData AND passes inquiryCount=0 (decisions run up to 100 pts higher than the disclosed score). For a licensed bureau this is a regulatory finding. Fix: one `assembleScoreInputs(borrowerId)` used by every surface.
**F2 — Empty alt-data rows grant a free +64 thin-file boost.** VERIFIED against code 2026-07-09: there are two separate alt-data bonus calculations in credit-score.ts — the thin-file path (`accounts.length === 0`) genuinely defaulted `altOnTimeRatio` to 0.8 when zero transactions were present (confirmed exploit: `{source:"mobile_money", totalTransactions:0}` × 2 sources → 600→664, exactly as described); the with-accounts path already defaulted to 0 for zero transactions but had no minimum-sample-size floor at all (3 sources × 1 fabricated on-time transaction → full +90 bonus). Fixed both: added a `MIN_ALT_TXNS_PER_SOURCE = 5` floor to both paths and removed the 0.8 default.
**B1 — Cross-lender tradeline tampering via batch upload.** Upsert key includes caller-supplied free-text `lenderInstitution` with no ownership check — Lender A can overwrite Lender B's delinquency (or fabricate one). Fix: derive lenderInstitution from uploader's org.
**B2 — Global borrower match + silent identity overwrite in batch.** nationalId matched across all countries/orgs, then name/phone/DOB unconditionally overwritten from the batch row. Typo'd ID = someone else's delinquencies + rewritten identity. Fix: scope matching, never auto-overwrite identity, route conflicts to review.
**A1 — monthSpan systematically overstates income.** N salaries span N-1 months (3 credits in 90 days → income ×1.5); income and expenses use different denominators → inflated disposable income → `maxRecommendedNewCredit` over-lends. Fix: normalize both by periodDays/30.
**A2 — Loan disbursements & savings withdrawals counted as income.** Borrowers cycling digital loans show rising "income" when most distressed. Fix: exclude these types from income candidates.
**A3 — Uploaded PDF overwrites borrower.monthlyIncome as trusted.** LLM-parsed statement (≥5 txns) writes back permanently, feeds decision DTI. Fabricated PDF → inflated income on record. Fix: remove bank_statement_pdf from write-back or gate behind maker-checker.

### MEDIUM
- **F3** creditLimit not sanitized → one bad value NaNs the utilization sum (booby-traps the F1 fix)
- **F4** ≤50% invalid account rows silently dropped (raises onTimeRatio) vs >50% → score 300; incoherent asymmetry
- **A4** multi-currency transactions summed without conversion (USD salary + GHS spend = meaningless DTI)
- **A5** LLM JSON extraction greedy-regex `/{[\s\S]*}/` — trailing brace in model output discards the whole statement silently; no bounds on hallucinated amounts
- **B3** batch updates bypass the maker-checker approval that single-account PATCH enforces
- **B4** `isValidMappingId` regex unanchored — legit IDs starting "NA"/"LB" rejected as synthetic
- **C2** account dedup only on the lender report path (by lowercased accountNumber) — other surfaces double-count duplicates
- **C4** XDS Ghana sandbox fallback (deterministic FAKE bureau data for real Ghana Cards) reachable in production — no NODE_ENV gate, persisted into reports tagged only source:"sandbox"

### LOW
- **F5** PEP flag has zero score effect (confirm as documented policy) · **F6** closed-in-arrears accounts count as clean history · **A6** two incompatible DTI definitions (monthly-service vs total-debt/annual-income with 999 sentinel) · **A7** "TIN" narrations classified as government benefits; rent credits mostly fall to business_income · **B5** garbage arrears CSV → parseInt||0 → "performing" · **B6** orphan thin-file borrowers on partial batch failure · **I3** appealed judgments cost nothing; ML model gets different judgment inputs per entry point · **C3** client methodology simulator can't match server for alt-data borrowers

### Priority fix order (scoring)
1. I2 + I1 — broken soft-pull + inquiry filtering (small diffs, big correctness)
2. F1/F3 + C1 — utilization column + single input assembler (CHANGES PUBLISHED SCORES → version the model, e.g. "UCH Scorecard v1.1")
3. B1/B2 — batch tampering & identity overwrite controls
4. A1/A2/A3 — affordability income-inflation trio
